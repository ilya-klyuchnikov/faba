Contract inference
==================

Some strategies/hints for contracts.

1. If parameter is not splitting and at least one call doesn't containt parameter as argument, then we are not inferring contract.